<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:P3PHelper.Models"
             x:Class="P3PHelper.Views.SLinks.AeonPage" >
    <ContentPage.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label Text="{Binding FemName}"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" />
                <Label Text="{Binding Arcana}" />
                <Label Text="" />
                <StackLayout x:Name="RankUpsContainer">
                
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>