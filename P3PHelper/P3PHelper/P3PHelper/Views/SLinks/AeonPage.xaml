<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:P3PHelper.Models"
             x:Class="P3PHelper.Views.SLinks.AeonPage" >
    <ContentPage.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter" />
        <!--Style for male Property titles in SLinks Arcana views-->
        <Style x:Key="MalePropertyTitles" TargetType="Label">
            <Setter Property="HorizontalTextAlignment"  Value="Center" />
            <Setter Property="FontSize" Value="25" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="TextColor" Value="Blue" />
        </Style>
        <!--Style for female Property titles in SLinks Arcana views-->
        <Style x:Key="FemalePropertyTitles" TargetType="Label">
            <Setter Property="HorizontalTextAlignment"  Value="Center" />
            <Setter Property="FontSize" Value="25" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="TextColor" Value="HotPink" />
        </Style>
        <!--Style for Property values in SLinks Arcana views-->
        <Style x:Key="PropertyValues" TargetType="Label">
            <Setter Property="HorizontalTextAlignment" Value="Center" />
            <Setter Property="TextColor" Value="Black" />
            <Setter Property="FontSize" Value="20" />
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0"  Text="{Binding Arcana}" HorizontalOptions="Center" FontSize="50" FontAttributes="Bold"
                   TextColor="Blue" />

            <ScrollView Padding="10" Grid.Row="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <!--Male Properties-->
                    <StackLayout Grid.Column="0" >
                        <Label Text="Name" Style="{StaticResource MalePropertyTitles}" />
                        <Label Text="{Binding MaleName}" Style="{StaticResource PropertyValues}" />
                        <Label Text="Unlock Date"  Style="{StaticResource MalePropertyTitles}" />
                        <Label Text="{Binding MaleUnlockDate}" Style="{StaticResource PropertyValues}" />
                        <Label Text="How to Unlock"  Style="{StaticResource MalePropertyTitles}" />
                        <Label Text="{Binding MaleHowToUnlock}" Style="{StaticResource PropertyValues}" />
                        <Label Text="Availability"  Style="{StaticResource MalePropertyTitles}" />
                        <Label Text="{Binding MaleAvailability}" Style="{StaticResource PropertyValues}" />
                        <StackLayout x:Name="MaleNeedPersonaContainer" >
                            <!--Requires Persona content-->
                        </StackLayout>
                        <StackLayout x:Name="MaleRankUpsContainer" >
                            <!-- Male RankUps content -->
                        </StackLayout>
                    </StackLayout>

                    <!-- Vertical line -->
                    <BoxView Grid.Column="1" BackgroundColor="Blue" WidthRequest="1" HorizontalOptions="Center" />

                    <!--Female Properties-->
                    <StackLayout Grid.Column="2" >
                        <Label Text="Name" Style="{StaticResource FemalePropertyTitles}" />
                        <Label Text="{Binding FemaleName}" Style="{StaticResource PropertyValues}" />
                        <Label Text="Unlock Date"  Style="{StaticResource FemalePropertyTitles}" />
                        <Label Text="{Binding FemaleUnlockDate}" Style="{StaticResource PropertyValues}" />
                        <Label Text="How to Unlock"  Style="{StaticResource FemalePropertyTitles}" />
                        <Label Text="{Binding FemaleHowToUnlock}" Style="{StaticResource PropertyValues}" />
                        <Label Text="Availability"  Style="{StaticResource FemalePropertyTitles}" />
                        <Label Text="{Binding FemaleAvailability}" Style="{StaticResource PropertyValues}" />
                        <StackLayout x:Name="FemaleNeedPersonaContainer" >
                            <!--Requires Persona content-->
                        </StackLayout>
                        <StackLayout x:Name="FemaleRankUpsContainer" Grid.Column="2">
                            <!-- Female RankUps content -->
                        </StackLayout>
                    </StackLayout>
                </Grid>
            </ScrollView>
        </Grid>
        
    </ContentPage.Content>
</ContentPage>