<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:P3PHelper.Models"
             x:Class="P3PHelper.Views.SLinks.AeonPage" >
    <ContentPage.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <ScrollView>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackLayout Grid.Column="0" >
                    <Label Text="{Binding MaleName}" HorizontalOptions="Center" />
                    <Label Text="{Binding MaleUnlockDate}" />
                    <Label Text="{Binding MaleHowToUnlock}" />
                    <Label Text="{Binding MaleAvailability}" />
                    <StackLayout x:Name="NeedPersonaContainer" >
                        
                    </StackLayout>
                    <StackLayout x:Name="MaleRankUpsContainer" >
                        <!-- Male RankUps content -->
                    </StackLayout>
                </StackLayout>
                

                <StackLayout x:Name="FemaleRankUpsContainer" Grid.Column="1">
                    <!-- Female RankUps content -->
                </StackLayout>
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>